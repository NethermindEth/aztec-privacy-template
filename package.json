{
  "name": "aztec-protocol-privacy-template",
  "private": true,
  "version": "0.0.0",
  "description": "Aztec privacy template scaffold",
  "license": "MIT",
  "scripts": {
    "fmt": "bunx biome format --config-path .biome.json --write .",
    "fmt:check": "bunx biome format --config-path .biome.json --write=false .",
    "lint": "bunx biome lint --config-path .biome.json .",
    "test": "bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "toml": "^3.0.0",
    "solhint": "^6.0.3"
  }
}
